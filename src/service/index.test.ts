import { decodeParams } from '.'
describe(' test api/index.js gen ', () => {
  it(' gen  ', () => {
    decodeParams('post /build/mobile/save')
    // console.log(APIFunction)
    expect(1).toBe(1)
  })
})
